# Thunderbird

## 最小化到托盘

在 `Settings | System Integration` 中勾选 `When Betterbird is minimized, move it to the tray`.

安装插件 [Minimize on Close](https://github.com/rsjtdrjgfuzkfg/thunderbird-minimizeonclose).

## 调整为垂直视图

在 `Menu | View | Layout` 中选择 `Vertical View`.
